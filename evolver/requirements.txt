# Python dependencies for EvoCode - LLM-guided evolution of code components
# Requires Python 3.12+

# Google Gemini API (for LLM-guided code generation)
google-genai>=0.2.0

# Environment variables (for API key management)
python-dotenv>=1.0.0

# OpenAI API (for ModelArts endpoints)
openai>=2.16.0

# For logging
loguru

# Graph plots - for backend data
networkx
numpy

# Note: JDK 21 is also required by the built-in Java domains (ails_vrp, vm_scheduling)
# to compile generated code. Not needed for domains that build in another language.
# Install via: apt install openjdk-21-jdk (Linux) or download from Adoptium
